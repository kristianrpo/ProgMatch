// This function is triggered when the window is fully loaded.
window.addEventListener('load', function() {
    // Find the HTML element with the class 'generalContainer'.
    var element = document.querySelector('.generalContainer');
    // Add the 'animate' class to the element to activate an animation.
    element.classList.add('animate');
});